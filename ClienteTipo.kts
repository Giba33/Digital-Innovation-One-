package one.digitalinnovatione.ditestesgitalone.

enum class ClienteTipo(desecricao: String) {
    pf(descricao "Pessoa Fisica"),
    pj(descricao "Pessoa Juridica"),

    companion object {
fun printDescricao()
    }
}
